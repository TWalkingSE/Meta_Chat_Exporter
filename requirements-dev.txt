# Dependências de desenvolvimento e qualidade
# Instale com: pip install -r requirements-dev.txt
# (ou: pip install -e ".[dev]")
#
# Para instalação reproduzível (versões fixadas), use os arquivos de lock:
#   pip install -r requirements.lock -r requirements-dev.lock

pytest>=9.0.0
pytest-cov>=5.0.0
ruff>=0.6.0
mypy>=1.11.0
pre-commit>=3.8.0
hypothesis>=6.100.0